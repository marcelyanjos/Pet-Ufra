import React, { useRef, useCallback } from 'react'
import { Image, ImageBackground, PixelRatio, SafeAreaView, StyleSheet, Text} from 'react-native'

export default function Home() {
    

    return ( 
        
            <SafeAreaView>
        <Text style={{color:'#323232'}}>Infos</Text>
</SafeAreaView>
        
       
    )
}